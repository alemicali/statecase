# Packages

Planned packages: domain, protocol, crypto, client, storage-local, adapters,
workspace, Drops, and sync core. They will be introduced in TDD delivery order;
empty package shells are intentionally not committed.
