# Packages

Implemented packages currently cover domain identity, protocol, crypto,
content-defined chunking, local SQLite storage, runtime scheduling/locking,
Codex and Claude adapters, and the sync coordinator core. Dedicated workspace,
Drop, and HTTP-client packages remain planned extractions from the current CLI
vertical slice; empty package shells are intentionally not committed.
